import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputTxt: { 
      borderWidth: 1,
      borderColor: '#ccc',
      width: '75%',
      padding: 10,
      borderRadius: 5,
      marginBottom: 5,
      maxWidth: 300
    }
})