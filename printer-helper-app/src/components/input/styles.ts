import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    inputTxt: { 
      borderWidth: 1,
      borderColor: '#ece2d2',
      width: '75%',
      padding: 10,
      borderRadius: 5,
      marginBottom: 5,
      maxWidth: 300
    },

    inputTitle: { 
      fontSize: 15,
      color: '#41423a'
    }
})